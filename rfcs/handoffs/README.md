# RFC Implementation Handoffs

This directory holds **implementation handoffs** for proposed RFCs. A handoff
translates an accepted RFC's design into developer-executable work: module layout,
PR boundaries, task breakdown, acceptance checklists, edge cases, and CI commands.

The RFC remains the design authority; a handoff never overrides it. Handoffs are
working documents for the implementation team and may be revised as work proceeds.

## Current handoffs (v0.20+ proposed set, RFC-033–042)

| Handoff | RFC |
|---|---|
| `033-implementation-handoff.md` | RFC-033 `matten-data` Beta-Decision and Scope Lock |
| `034-implementation-handoff.md` | RFC-034 `matten-data` Table Model and Public API Boundary |
| `035-implementation-handoff.md` | RFC-035 CSV Ingestion, Schema Summary, Missing Values, and Numeric Conversion |
| `036-implementation-handoff.md` | RFC-036 `matten-data` Examples, Documentation, and Release Gate |
| `037-implementation-handoff.md` | RFC-037 Deferred Streaming and Large CSV Policy |
| `038-implementation-handoff.md` | RFC-038 Core Numeric Comfort APIs |
| `039-implementation-handoff.md` | RFC-039 Shape Composition API Boundary |
| `040-implementation-handoff.md` | RFC-040 Small Statistics Boundary — Core vs Companion |
| `041-implementation-handoff.md` | RFC-041 Linear Algebra Boundary — Core Lite vs External Crates |
| `042-implementation-handoff.md` | RFC-042 Pandas-Inspired Scope Guard for `matten-data` |
| `063-phase-1-visual-docs-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 1 docs only |
| `063-phase-2-example-reports-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 2 example reports |
| `063-phase-3-local-report-tool-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 3 first local report tool slice |
| `063-phase-3-shape-flow-report-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 3 shape-flow local report slice |
| `063-phase-3-dynamic-readiness-report-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 3 dynamic-readiness local report slice |
| `063-phase-3-mlprep-standardization-report-handoff.md` | RFC-063 Visual Understanding and Reporting — Phase 3 mlprep-standardization local report slice |
| `026-037-large-data-streaming-policy-closure-handoff.md` | RFC-026 / RFC-037 Large Data and Streaming Policy — proposed policy-closure slice |
| `049-lifecycle-closure-handoff.md` | RFC-049 lifecycle closure / Phase 4 deferral handoff |
| `054-matten-migrate-readiness-audit.md` | RFC-054 `matten-migrate` readiness audit |
| `054-matten-migrate-first-tool-handoff.md` | RFC-054 `matten-migrate` first local advisory tool slice |
| `054-matten-migrate-target-suggestion-handoff.md` | RFC-054 `matten-migrate` target-specific suggestion slice |
| `054-matten-migrate-explain-api-handoff.md` | RFC-054 `matten-migrate` static API explanation slice |
| `054-matten-migrate-check-bridges-handoff.md` | RFC-054 `matten-migrate` bridge-readiness check slice |
| `054-lifecycle-closure-handoff.md` | RFC-054 lifecycle closure / future automation extraction handoff |
| `066-v1-readiness-audit-handoff.md` | RFC-066 v1.0 readiness audit report handoff |
| `065-educational-visualization-handoff.md` | RFC-065 Educational Visualization and Tensor Learning Path — first docs/positioning slice |
| `065-phase-2-educational-shape-data-path-handoff.md` | RFC-065 Educational Visualization and Tensor Learning Path — Phase 2 educational shape/data docs slice |
| `065-phase-3-educational-path-report-handoff.md` | RFC-065 Educational Visualization and Tensor Learning Path — Phase 3 local educational-path report slice |
| `068-local-html-educational-artifact-handoff.md` | RFC-068 Rich Local Visualization Artifacts — first local HTML educational artifact slice |
| `068-shared-educational-report-model-handoff.md` | RFC-068 Rich Local Visualization Artifacts — shared educational-path report data refactor slice |
| `068-shape-flow-html-artifact-handoff.md` | RFC-068 Rich Local Visualization Artifacts — shape-flow local HTML artifact slice |
| `068-post-032-visualization-continuation-audit.md` | RFC-068 post-0.32 visualization continuation audit and next-slice recommendation |
| `068-dynamic-readiness-html-artifact-handoff.md` | RFC-068 Rich Local Visualization Artifacts — dynamic-readiness local HTML artifact handoff |
| `068-post-033-visualization-continuation-audit.md` | RFC-068 post-0.33 visualization continuation audit and next-slice recommendation |
| `068-mlprep-standardization-html-artifact-handoff.md` | RFC-068 Rich Local Visualization Artifacts — mlprep-standardization local HTML artifact handoff |
| `068-post-034-visualization-gap-audit.md` | RFC-068 post-0.34 visualization gap audit and next-decision recommendation |
| `068-data-readiness-html-artifact-handoff.md` | RFC-068 Rich Local Visualization Artifacts — data-readiness demo-only local HTML artifact handoff |
| `068-post-035-fixed-demo-html-closure-audit.md` | RFC-068 post-0.35 fixed-demo local HTML closure audit |
| `069-input-mode-html-policy-audit.md` | RFC-069 input-mode HTML report policy audit |
| `069-input-mode-html-implementation-handoff.md` | RFC-069 input-mode HTML report implementation handoff |
| `069-post-036-input-mode-html-closure-audit.md` | RFC-069 post-0.36 input-mode HTML closure audit |
| `070-public-visualization-report-readiness-audit.md` | RFC-070 public visualization/report readiness audit |
| `070-private-report-model-extraction-handoff.md` | RFC-070 private report-model extraction handoff |
| `070-json-report-schema-policy-audit-handoff.md` | RFC-070 JSON report-schema policy audit handoff |
| `070-json-report-schema-policy-audit.md` | RFC-070 JSON report-schema policy audit |
| `070-fixed-demo-json-report-implementation-handoff.md` | RFC-071 fixed-demo private JSON report implementation handoff, drafted from the RFC-070 audit line |
| `070-post-037-public-visualization-closure-audit.md` | Accepted RFC-070 post-0.37 closure audit; recommends a separate modularization RFC |
| `072-matten-report-modularization-implementation-handoff.md` | Retained accepted RFC-072 implementation record; all phases reviewed and committed, lifecycle closed |
| `073-private-input-mode-json-implementation-handoff.md` | Accepted RFC-073 handoff; bounded private data-readiness input JSON implemented, reviewed (GO, no conditions), and released in `0.38.0` |
| `076-v1-release-preparation-handoff.md` | Accepted RFC-076 handoff; sequences the `1.0.0` release-preparation change (RFC-067 maturity table, compatibility.md rewrite, 19-site pre-1.0/0.x sweep, 29-string current-family retarget, version bump); reviewed (GO, no conditions); execution deferred pending pre-v1 feature work (RFC-077, RFC-078); no implementation authorized |
| `077-seeded-train-test-split-handoff.md` | Accepted RFC-077 handoff; `train_test_split_seeded` for `matten-mlprep` (SplitMix64, Fisher-Yates over row indices, locked-permutation test); implemented and reviewed (GO, no conditions, reproducibility contract proven by mutation), committed `4c554a4`; no version bump or release |
| `078-matten-stats-companion-handoff.md` | Accepted RFC-078 handoff; new `matten-stats` companion crate (fifth published crate, Experimental maturity): `covariance`/`correlation` (sample, `ddof = 1`) and `quantile` (linear interpolation); implemented and reviewed (GO, no conditions, ddof policy proven by mutation), committed `7f1cbba`; no version bump or release |
| `079-0390-pre-v1-feature-release-handoff.md` | Accepted RFC-079 handoff; `0.39.0` release-prep sequencing RFC-077 and RFC-078; reviewed (GO, no conditions); owner confirmed the version bump and deferred `matten-stats`'s first publish pending an external `ddof` read, narrowing the release to RFC-077 only; release-prep committed, then `0.39.0` tagged and published outside this project's assistant session — `matten-stats` was published anyway, corrected in the post-release alignment (`0390-post-release-alignment-handoff.md`) |
| `0390-post-release-alignment-handoff.md` | Post-release truth-alignment handoff (not tied to a single new RFC): determined via crates.io (never `cargo publish`) that `matten-stats` was published at `0.39.0` despite RFC-079 §3's deferral decision (Case B); corrected RFC-079/RFC-078 status, added a dated CHANGELOG note without erasing the original entry, and added a new ROADMAP history row leaving `3.25.0` intact; documentation only, no code/version/tag/publish action |
| `080-matten-mlprep-production-ready-handoff.md` | Accepted RFC-080 handoff; promotes `matten-mlprep` candidate → production-ready, closing RFC-058 §5.1's Option B exit criterion via RFC-077; review corrected an over-broad seven-file sites list to six verified sites, implementation found a seventh (`src/lib.rs`'s Status doc comment); added and proved a new "must not say candidate" guard; label-only, no code/API/version/release |
| `081-v1-family-experimental-crate-policy-handoff.md` | Rereviewed RFC-081 handoff (GO, one fix applied); refreshes RFC-076's stale inventory. First draft's five-site list was replaced with an actual sweep-and-classify pass (17 real sites, not 5); implemented and committed. RFC-076 stays in `proposed/`, now blocked on both `matten-stats` resolving an RFC-081 Exit A/B decision and RFC-081 §5's own reasoning receiving the owner's disposition (external read, narrowed rule, or accepted as argued) |
| `082-streaming-csv-batches-handoff.md` | Accepted RFC-082 handoff, after a first draft's mistaken guard-narrowing claim was fact-checked and dropped (`scripts/check-streaming-scope.sh` already permitted `CsvBatchReader` unmodified — verified via regex test, example-name test, and an end-to-end fixture run). Implements `CsvBatchReader` behind `matten-data`'s off-by-default `streaming` feature; 13 new tests including an equivalence test and a batch-boundary line-number-parity test; no code change to the other four crates, no version bump, no release |
| `083-matten-stats-expansion-handoff.md` | Accepted RFC-083 handoff; implemented and reviewed (approved, no corrections). Adds `covariance_population`, `skewness`, `kurtosis` to `matten-stats` (3 public functions → 6). Additive only: no new error variant, no new dependency, no feature gate, no version bump, no release, and no maturity change — `check-release-docs.sh` (lines 120/124) asserts the `Experimental` label and must keep passing unmodified. Opens with a mandatory pre-coding check: RFC-083 §4.1's SciPy/pandas estimator-default claims are explicitly marked unverified, and a mismatch is an escalation, not a local fix |
| `084-promote-matten-stats-production-ready-candidate-handoff.md` | Accepted RFC-084 handoff; implemented and reviewed (approved after one correction — the promotion guard's first check was over-broad and rejected legitimate maturity-history prose). Part of PART 1 landed before acceptance and was authorized retroactively. Two ordered parts: Part 1 adds the missing `matten-stats` CI job (including `--features dynamic`, without which the crate's one dynamic test stays MSRV-only) and its four example smoke runs; Part 2 moves the label and inverts `check-release-docs.sh`'s `Experimental` assertion rather than deleting it. Requires a deliberate-failure proof that the inverted guard can actually fail, and requires deriving the label-site list by sweep-and-classify rather than trusting the handoff's enumeration — three prior RFCs shipped incomplete site lists. `rfcs/done/`, `rfcs/handoffs/`, `CHANGELOG.md` and `docs/design/history/` are must-not-touch |
| `085-promote-matten-data-production-ready-handoff.md` | Accepted RFC-085 handoff; implemented and reviewed (approved over two rounds of guard corrections). Single part (no CI gap to close, unlike RFC-084): invert the guard's `matten-data` label assertion, narrow the line-89 blanket `experimental` grep that RFC-084 review C1 already required narrowing for `matten-stats`, move the label at every live site by sweep-and-classify, and add the streaming-scope note in three places. Requires **three** deliberate-failure proofs, not one — the headline trap is that `"production-ready candidate"` *contains* `"production-ready"`, so a naive positive check passes on stale text and cannot detect the exact staleness it exists to catch. Labels, docs and one guard only; the RFC-042 scope lock must pass unmodified |
| `086-0400-feature-and-maturity-release-handoff.md` | Accepted RFC-086 handoff; implemented and reviewed (approved, no corrections); `0.40.0` tagged and published to crates.io 2026-07-30. Release preparation only: version bump, 36 measured version-string retargets (re-measure, never `sed` repo-wide — 132 historical occurrences must not move, the exact damage RFC-079's review caught), and the `[0.40.0]` CHANGELOG entry. Three easy-to-get-wrong content requirements: `kurtosis` is **excess**, `CsvBatchReader` is **not** equivalent to `from_csv_path` (two documented divergences), and `matten-stats` ships as production-ready **candidate**. Also fixes the CHANGELOG preamble, which still names four crates. Explicitly forbids tagging and publishing — both are owner actions and tagging is blocked by RFC-086 §3 |

| `087-repeat-tile-meshgrid-handoff.md` | Accepted RFC-087 handoff; implemented and reviewed (approved, no corrections) — two of its own instructions were found wrong against the codebase and correctly not followed. Eight functions in `composition.rs`, mirroring `concatenate`/`stack`'s paired `try_`/panicking shape and reusing `reject_dynamic` and `MattenLimits::check_shape` rather than recomputing sizes. Two tests carry the slice: exact-value `repeat` vs `tile` (the classic swap), and **`meshgrid` with inputs of unequal length** — with equal lengths `xy` and `ij` differ only by a transpose, so a wrong implementation passes silently, which is the exact failure RFC-087 §5 chose `xy` to avoid. Mandates the `public-api-snapshot.md` update and treats the example as a deliverable, since the slice is justified on teaching value |

| `088-negative-slice-indices-handoff.md` | Accepted RFC-088 handoff; implemented and reviewed (approved after one should-fix). A parser and validation change inside `crates/matten/src/slice.rs`, behind an existing signature: **no public item changes, so `public-api-snapshot.md` must NOT move**. Design is resolve-then-validate — RFC-008 §12.2's bounds rules stay untouched, only the values fed to them are resolved. The load-bearing test is rank-2 with **unequal** dimensions (`[3, 2]`): an implementation resolving every axis against axis 0's size passes a rank-1 suite completely, the same failure shape as RFC-087's equal-length `meshgrid` trap |

| `089-0410-core-shape-and-slicing-release-handoff.md` | Accepted RFC-089 handoff; implemented, reviewed, and released as `0.41.0` on 2026-07-31. Release preparation only: version bump, 37 measured retargets, and the `[0.41.0]` CHANGELOG entry. Measure with `0\.40\b`, never the suffixed form — the suffixed pattern misses two sites that need **content** updates rather than a number swap, `introduction.md` (still describing RFC-086's release) and `public-api-snapshot.md`'s framing sentence. Five CHANGELOG claims are called out as publishable falsehoods if got wrong, the headline being that **none of this is "NumPy-compatible"** — `tile`'s rank rule and negative indices' non-clamping both diverge deliberately |

| `090-histogram-bin-policy-handoff.md` | Accepted RFC-090 handoff; implemented and reviewed (approved after one correction; the handoff's own edge formula was found unsound and correctly refined). One new module in `matten-stats`, one new error variant (`InvalidBinCount`, modelled on `matten-data`'s `InvalidBatchSize`), and the crate's first use of `MattenLimits`. The load-bearing test is the **sum invariant** — `counts.iter().sum() == x.len()` with a maximum landing exactly on the final edge, which is what proves the closed last bin; without it an off-by-one silently drops the max. Also names the **five sites across three files** carrying the old `Tensor -> f64` framing, since stopping at the first per file would leave the crate documenting a rule its own API breaks |
| `091-0420-statistics-release-handoff.md` | Accepted RFC-091 handoff; implemented and reviewed (approved after one correction, which was **outside** everything this handoff named — see below). Release preparation only — bump, retarget, CHANGELOG; **no tag, no publish**. Names the three ways this release differs from `0.41.0`, each a way to get it wrong by copying that one: a **mandatory `Changed` section**, because RFC-090 altered `ZeroVariance` and `NonFiniteValue` messages on functions that already shipped, so "we only added a function" is false; `introduction.md` needs a **content** update naming RFC-090 while `public-api-snapshot.md` must get the **number only**, since core's surface is untouched and citing RFC-090 there would be a false claim; and the gate set is now **seven** guards, `check-doc-code.sh` having arrived since the last release. Also warns that the §5 count is stale on arrival — the RFC and handoff add their own `0.41` occurrences — and that a `^\./` exclusion may not match what the local grep emits — both warnings paid off, the re-measure landing at 36/16 against the RFC's 37/17 once `Cargo.toml`'s own site resolved by the bump. The correction review found was on `docs/src/introduction.md`, which **neither this handoff nor RFC-091 listed as needing care beyond its number**: its inherited *"no other runtime behavior change"* clause was true for `0.41.0` and false here. The lesson for the next release handoff is that a sentence rewritten around a version number must be re-read as a whole, not just retargeted |
| `093-browser-shape-playground-handoff.md` | Accepted RFC-093 handoff; implemented and reviewed (approved after one correction). Leads on the scope lock — **text only, no pixels that represent data** — because the path from this page to the `matten-viz` crate RFC-070 refused is short and every step looks reasonable. Names the three registration steps whose omission is silent: `workspace.exclude` (or `wasm-bindgen` enters the shared lockfile), the `.gitignore` lock entry, and the `SUMMARY.md` entry (or mdBook never renders the page). Says explicitly which logic belongs in Rust and which in JS, because RFC-093 §7 records that **no guard can check an interactive page** — keeping the glue thin is the only mitigation available. Requires `check-doc-code.sh` under `RUSTFLAGS="-D warnings"` per rule 002 §8, the exact omission that broke CI the same day |
| `095-matrix-grid-rendering-handoff.md` | Accepted RFC-095 handoff; implemented and reviewed (approved after one correction, the nav repetition it had itself flagged). Opens by stating that **the scope rule already changed** — RFC-093 §6 was amended at acceptance to representation-permitted / visualization-forbidden — and immediately that the new rule is **narrower**, not a relaxation: it newly bans ASCII charts the old *"text only"* wording allowed. Names the three `matten-report` display constants to inherit verbatim, including the `-0.000` clamp, because RFC-093 §8 would put both surfaces on one site and the playground crate cannot import from the tool. Requires **exact-string** tests including padding, since a mis-padded column still renders and merely misleads, plus a rank-3 regression test proving the flat form is untouched — that boundary is a decision, not a deferral. Also requires rewriting `docs/src/playground.md`, which quotes the superseded rule; leaving it would repeat the `introduction.md` contradiction found during `0.42.0`. **Widened 2026-08-02** with §5a: the page puts 28 lines of contributor prose above the first input, has no CSS at all, and is linked from nowhere — forms move to the top, the build note and scope rule to a contributors section at the bottom, and a stylesheet arrives that must use mdBook theme variables and must not encode any value, CSS being a medium like any other under §6 |
| `096-example-grid-rendering-handoff.md` | Accepted RFC-096 handoff; implemented and reviewed (approved after one correction, which the implementation had itself asked about). Leads on the condition the design rests on rather than the format: the example **must assert its own rendered block**, because a local formatter has no unit tests available to it — a `#[test]` in an example runs 0 tests — and CI catches a panic but not a mis-aligned grid, which renders and exits 0. States that rendering correctly without asserting has not done the job. Warns twice against the instruction most likely to be helpfully ignored: **do not copy the playground's constants**, since 12-column truncation is dead code at 3 columns and `{:.3}` puts three digits of noise in every cell of hand-chosen values — a deliberate divergence, not an inconsistency to fix. Unlike RFC-093 and RFC-095 this one **deploys nothing**; it reaches users only at the next release |
| `097-report-demos-on-the-site-handoff.md` | Accepted RFC-097 handoff; implemented and reviewed (**approved, no corrections**); its §2 table carries an inline correction, being the one instruction that was wrong. States the **order** first — fix the renderer, then generate — because generating from the current renderer commits the defect and the fix then lands as a diff in files meant to be mechanically derived. Leads on the judgement most likely to be over-applied: of 13 flat value lists across the five demos, **most are correct**, since a rank-1 `mean_axis` result genuinely is a list; only rank-2 tensors are the defect. Requires the generated pages to be **committed, not git-ignored** — mdBook creates a missing page empty and exits 0, so ignoring them would deploy blanks silently — with a new `check-report-demos.sh` proving drift, and forbids a guard that regenerates in place, since one that silently fixes drift hides it. The index page is **hand-written**, because it must state what the tool is *not*: a fixed demo set, `publish = false`, no published reporting product (RFC-070) |
| `099-result-form-matmul-handoff.md` | Accepted RFC-099 handoff; implemented and reviewed (**approved, no corrections**). Opens by stating this is **robustness, not a feature** — nothing new becomes possible, and every existing message must survive byte-identical. Leads on the single most likely mistake, quoted from **captured panics rather than source**: `dot`'s dynamic guard is bespoke, so calling the obvious `reject_dynamic` silently rewords a user-visible message, changing both operation name and text. Points at the net that already exists — RFC-095 C1's live `catch_unwind` tests and example 57's assertions — and says that **editing either to make it pass means the net is catching you**, not that a test needs updating. Requires the dynamic message to be pinned by a new test, since it is the one string nothing currently asserts, and requires both feature profiles, the guard being `#[cfg(feature = "dynamic")]` |
| `100-display-for-tensor-handoff.md` | Accepted RFC-100 handoff; implemented and reviewed (approved after one correction, which resolved a contradiction inside the RFC itself). States that **the contract is the work** — the formatter is short, but `{}` is a permanent commitment — and names the one decision deliberately **not** made at acceptance: whether `{:#}` means untruncated, to be decided, implemented and reported rather than assumed from the RFC's stated lean. Leads on the float trap: `{:?}` per cell so a whole number renders `1.0`, since ndarray's `Display` drops the `.0` and matching it would reintroduce RFC-096 C1 verbatim. Warns that both migrated files assert exact output, so **editing an expected string means either `Display` is wrong or you are undoing RFC-095/096**, and that `check-report-demos.sh` failing is the guard working rather than a test to update. The report tool mostly stays: its `format_cell` closure serves two `{:.3}` sites a fixed `Display` cannot |
| `101-0430-core-surface-release-handoff.md` | Accepted RFC-101 handoff; prepared and reviewed (**approved, no corrections**); the tag and publish remain separate owner authorizations. Release preparation only — bump, retarget, CHANGELOG; **no tag, no publish**. Its §2 exists because the implementer has a recent, well-documented release to pattern-match against and **two things invert from it**: this release is **Added-only**, so no `Changed` section and explicitly no empty heading to mirror `0.42.0`; and `public-api-snapshot.md` needs a **content** update where RFC-091 §5 required the number only, because core's surface genuinely changed this time. Folds in RFC-093's O1 carry-forward, now due, into that same paragraph edit. Names the four CHANGELOG claims that would be publishable falsehoods, the headline being that **`Display` deliberately renders `1.0` where ndarray renders `1`**. Notes the gate set is now **eight** guards, and that publishing is one `cargo publish --workspace` rather than RFC-091 §7's superseded five-crate sequence |
| `102-slicing-on-dynamic-tensors-handoff.md` | Accepted RFC-102 handoff; implemented and reviewed (approved with one required correction, which was the **specification's gap, not the implementation's**). Opens by stating the theme is **wiring, not design** — and that the ROADMAP framing it replaces was mine and wrong for the third time — then carries a ten-row evidence table pairing every factual claim to the command or `file:line` that established it. Leads on E9, the trap where `get_element` resolves through the **outer** shape, so a wrong wrapper shape misreads silently with no panic and no error. Names three risks invisible to value assertions (copy-instead-of-share, nested-instead-of-composed views, push-built index vector under rank collapse) and forbids weakening those tests to value equality. **Two handoff defects were found by the implementer and are corrected inline**: it required a `CHANGELOG.md` entry RFC-102 §7 does not list — release-prep RFCs own that file — and it stated no RFC-vs-handoff priority rule at all, which is why the disagreement had to be resolved rather than raised. Review then found the risk **nobody** specified: a slice retains its source's entire allocation, with `materialize()` `pub(crate)` and no documented escape hatch |
| `103-0440-dynamic-slicing-release-handoff.md` | Accepted RFC-103 handoff; prepared and reviewed (**approved, no corrections**); the tag and publish remain separate owner authorizations. Release preparation only — bump, 38 pin retargets, CHANGELOG, two content rewrites; **no tag, no publish**. Its §2 exists because the implementer has a four-day-old release to pattern-match against and **three things invert from it**: `0.43.0` was Added-only and forbade an empty `Changed` heading, so this one is **Changed-only** and must not emit an empty `Added` heading; `0.43.0` changed no behaviour, whereas this release *is* a behaviour change, making `introduction.md`'s *"No existing behavior changed"* a falsehood on the docs' front page; and the API snapshot needs a **sentence with no new row**, per the RFC-088 precedent already on that page, since RFC-102 changed no signature. Separates **38 live pins from 21 historical narrations** and requires the five record files to be asserted unchanged — the check RFC-101's own §5 table failed. Records that the author's first measurement was **wrong twice**, both under-counting, and instructs re-derivation with a mismatch treated as more valuable than the retarget. Names the CHANGELOG's most skippable rule: sharing must not ship without its retention cost |
| `104-mutable-element-access-handoff.md` | Accepted RFC-104 handoff; implemented and reviewed (**approved, no corrections**). Adds `get_mut`/`get_flat_mut`/`get_element_mut`, mirroring the existing getters. Opens by telling the implementer that **the RFC was wrong twice before it was right** — specified first as `set`/`set_flat` returning `Result`, then with a coercion decision falsely blocking the dynamic half — and that its probe output is **quoted from reverted scratch code, a claim to re-verify rather than a result**. Leads on the ordering trap: the bounds check must precede `materialize()`, or an out-of-range call silently pays for a full copy and still returns `None` — a performance bug with no visible symptom. Requires the dynamic panic to be asserted **against its message**, not merely that it panics, and pairs T8/T9 so that `materialize()` is proven *used correctly* rather than *used unconditionally*: (a) source unchanged alone passes if it always copies, (b) `Arc` identity broken alone passes if it never shared. Requires non-square test tensors, since square ones hide a coord/flat transposition |
| `105-empty-tensor-reduction-semantics-handoff.md` | Accepted RFC-105 handoff; implemented and reviewed (**approved, no corrections**; two of its own factual claims corrected by the implementer). Five reductions misbehave on an empty tensor that is **reachable today** without any constructor accepting one: `argmin`/`argmax` raise a **raw Rust slice panic**, defeating the `try_` form outright, and `mean`/`min`/`max` return `NaN`/`inf`/`-inf` sentinels — silent wrong answers. The fix is already written twice in-crate: `try_var`/`try_std` return a proper `InvalidArgument`, so the task is to make five siblings match, with no new variant and no signature change. Leads on the fixture trap — **every test must use a sliced-empty tensor**, because one cannot be constructed, and a fixture with `len > 0` makes every new test pass vacuously. Requires the panic **message** to be asserted, since the defect is which panic. Names three code comments that justify skipping an empty check by citing an invariant that is false. Carries one genuinely open question — `try_sum` returns `-0.0` where Rust gives `0.0`, observed and unexplained, to be investigated rather than fixed blind |
| `106-zero-sized-shape-model-audit-handoff.md` | Accepted RFC-106 handoff; audit **complete and accepted with one correction**; `git diff` was empty as required. Produces a document, not a diff — **`git diff` must be empty**, and a fix mid-audit would make the table describe code that no longer exists. Classifies every public operation in core and all four companions as CORRECT / MECHANICAL / SEMANTIC / INCONSISTENT, and **the SEMANTIC count is the deliverable**: it tells the owner whether accepting zero-sized dimensions is one RFC or a multi-release theme, a decision they have provisionally made but not finalised. Requires every operation to be **run, not read** — `min() == inf` was invisible in source, being a fold identity leaking out. Warns explicitly against understating the SEMANTIC count, since an understated one produces a wrong owner decision, and invites the implementer to report if the RFC's own premise fails, four having already failed in this area |
| `107-architecture-and-data-model-docs-handoff.md` | Accepted RFC-107 handoff; implemented and reviewed (approved after two corrections). Docs only — `git diff` must show no `.rs`, no `book.toml`, no workflow. Adds the two pages the book lacks: a reader-facing architecture overview and a data-model/lifecycle page. Opens by naming what makes the task different — these document a **shape**, not an API, so **every structural claim must cite what establishes it**, since this book already contains claims that were true when written and silently went false (`is_empty()`'s rationale and `stats.rs`'s *"not constructible in practice"*, both found this week). Leads on the architecture finding: the dependency graph is a **star, not a stack**, enforced from both directions by RFC-078 §6 and `check-core-dependency-boundary.sh`. Requires the sentence the library has never stated plainly — **`Tensor` is one type with two modes** — with its `cfg` gate named so default-feature readers are not misled. Collects the storage state machine and **both** of its consequences (a slice retains its source's whole allocation; mutating it releases that allocation) which currently sit in two method doc comments written hours apart. **Exactly two mermaid blocks**, which render as code blocks today and become diagrams if a renderer is ever enabled, with zero rewrite — and explicitly forbids adding `mdbook-mermaid`, a decision deliberately deferred |
| `108-empty-tensor-defects-stage-1-handoff.md` | Accepted RFC-108 handoff; implemented and reviewed (approved after corrections; the RFC's own scope was unsatisfiable and was amended). Two independent fixes from RFC-106's audit, neither depending on the zero-sized policy: `mm_mul`'s `chunks_mut(p)` panic with `p == 0` — **a live raw panic in published `0.44.0` that escapes the very `Result` RFC-099 added to prevent it** — and `is_empty()`, declined at two sites on a premise that is false, one of which defers to *"a future zero-sized-tensor RFC"* that has now run. Leads on the regression trap: `n == 0` and `m == 0` are **already correct**, so a uniform rewrite of `mm_mul`'s empty handling is how they get broken — T2/T3 assert them explicitly even though nothing about them changes. Requires the answer for `p == 0` to be shape `[m, 0]`, not `[0, 0]` and not an `Err`, and forbids re-adding the clippy allow to silence a complaint rather than fixing its cause. Both `is_empty()` sites must change together, since fixing one and leaving the other reproduces the split that hid this |
| `109-0450-mutation-and-empty-tensor-release-handoff.md` | Accepted RFC-109 handoff; prepared and reviewed (**approved, no corrections**); the tag and publish remain separate owner authorizations. Release preparation only — bump, 38 pin retargets, CHANGELOG, two content rewrites; **no tag, no publish**. Its §2 exists because the implementer has **two** recent releases to pattern-match against and **each is wrong in a different direction**: `0.43.0` was Added-only and forbade an empty `Changed` heading, `0.44.0` was Changed-only and forbade an empty `Added` one, and this release needs **three non-empty sections**. The API-snapshot instruction likewise **inverts**: RFC-103 said a new row would be a defect because RFC-102 changed no public item; here RFC-104 and RFC-108 added four, so omitting them is the defect. Splits **38 live pins from 41 records** and requires the records asserted unchanged — but corrects the invariant this project got wrong once: `CHANGELOG.md` legitimately *gains* an occurrence in the bump line, so assert **no removed line**, not a fixed count. Requires the **workspace all-features** clippy form CI actually runs, after RFC-108's review found a red branch that a narrowed gate had hidden |

## Documentation-governance handoffs

These translate the specs supersession analysis into design-team work. They are not tied to a
single RFC; their authority is the supersession map. Run in order: 01 (gap closure) before 02
(archival); 03 (philosophy) may run after 01.

| Handoff | Purpose |
|---|---|
| `docs-governance-01-spec-coverage-gap-closure-handoff.md` | Resolve the three unowned spec fragments (perf targets, golden/fuzz/property tests, `Display`) before archival |
| `docs-governance-02-spec-archival-and-ownership-rule-handoff.md` | Archive the v0.19.0 specs as tracked banner-marked history; declare the ownership rule |
| `docs-governance-03-philosophy-distillation-handoff.md` | Expand `docs/src/philosophy.md` into a guarded, evergreen principles page distilled from the specs |

Some handoffs are planning records and some are retained implementation records.
A handoff alone does not authorize new work; each new implementation slice still
requires the corresponding RFC acceptance or follow-up review before coding begins.
